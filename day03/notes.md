
- list operations: `extend` `append` `insert` `remove` `pop` `clear` `index` `count` `sort` `reverse` `enumerate`(forindex and value output) `zip` (for combining lists) `filter` (for filtering elements) `map` (for applying a function to each element) `reduce` (for reducing a list to a single value). `join` (for joining elements into a string) `split` (for splitting a string into a list) `copy` (for creating a shallow copy of a list) `deepcopy` (for creating a deep copy of a list).

